import type { AnimationSequence } from '../graph/types.js';

export function generateAnimationScript(_sequence: AnimationSequence): string {
  // Stub — will be implemented in Phase 1
  return '/* animation engine placeholder */';
}
